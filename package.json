{
  "scripts": {
    "start:backend": "cd back && node --watch server",
    "start:frontend": "cd front && npm run dev",
    "start": "npm-run-all --parallel start:backend start:frontend"
  },
  "dependencies": {
    "@heroicons/react": "^2.1.5",
    "@huggingface/inference": "^2.8.1",
    "dotenv": "^16.4.5",
    "filter-obj": "^6.1.0"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
